div    
    form(action="#").form&attributes(attributes)
        h1.form__title #{title}
        . 
            !{loginInput}
        . 
            !{passwordInput}
        . 
            !{loginError}
        . 
            !{button}
        . 
            !{link}